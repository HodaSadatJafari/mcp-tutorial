[project]
name = "mcp-tutorial"
version = "0.1.0"
description = "How to create and use MCP"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=2.1.1",
    "openai>=3.7.0",
]
